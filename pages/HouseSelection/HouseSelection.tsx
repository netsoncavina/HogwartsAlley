import { StyleSheet, Text, View } from "react-native";
import React from "react";

const HouseSelection = () => {
  return (
    <View>
      <Text>HouseSelection</Text>
    </View>
  );
};

export default HouseSelection;

const styles = StyleSheet.create({});
